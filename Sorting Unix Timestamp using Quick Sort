#include <stdio.h>

void swap(long long* a, long long* b) {

    // swap two elements
    long long temp = *a;
    *a = *b;
    *b = temp;
}

int partition(long long arr[], int low, int high) {

    // place pivot in correct position and rearrange array
    long long pivot = arr[high];
    int i = low - 1;

    for (int j = low; j < high; j++) {
        if (arr[j] <= pivot) {
            i++;
            swap(&arr[i], &arr[j]);
        }
    }

    swap(&arr[i + 1], &arr[high]);
    return i + 1;
}

void quickSort(long long arr[], int low, int high) {

    // perform partition if segment is valid
    if (low < high) {
        int pi = partition(arr, low, high);

        // recursively sort left and right partitions
        quickSort(arr, low, pi - 1);
        quickSort(arr, pi + 1, high);
    }
}

int main() {

    int n;

    printf("Enter number of log entries: ");
    scanf("%d", &n);

    if (n <= 0)
        return 0;

    long long logs[n];

    printf("Enter %d Unix timestamps:\n", n);

    for (int i = 0; i < n; i++) {
        scanf("%lld", &logs[i]);
    }

    quickSort(logs, 0, n - 1);

    printf("Chronologically Sorted Timestamps:\n");

    for (int i = 0; i < n; i++) {
        printf("%lld\n", logs[i]);
    }

    return 0;
}
//input:
Enter number of log entries: 5
Enter 5 Unix timestamps:
1693456789
1693000000
1694001234
1693205678
1693101111
